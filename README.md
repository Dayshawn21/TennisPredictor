# SportApp

Monorepo for sports predictions and features.

Layout created by GitHub Copilot assistant. Extend the FastAPI service under `api/app` and the React UI under `web`.

See `api/README.md` for running the API locally.
